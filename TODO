- presentacion del codigo

# HTML
  - $0 y $(), $$(), $_
  - live expressions
  - copy
  - show in viewport
  - break on: attribute modification, subtree modification, node removal
  - force state
  - color picker, contrast ratio, shadow, subic bezier.
  - styles, computed, event listeners (maybe layers panel)
  - eager evaluation
  - changes & animations tab

# Network
  - overview
  - filmstrip
  - copy as cURL, copy as fetch

# Source
  - client breakpoints + conditionals
  - break on exception
  - watch
  - callstack
  - debug function, local function, time + timeEnd, debugger breakpoints
  - server beakpoints (--inspect, --inspect-brk)
